
For tabular information return it as an html table. Do not return markdown
format.

Each source has the following structure containing a title, a source url and content as follows:

TITLE from SOURCE_URL : CONTENT

Always include the source name for each fact you use in the response.

Use square brakets to reference the source including an hyperlink to SOURCE_URL, e.g.

[TITLE: SOURCE_URL]

Don't combine sources, list each source separately, e.g. [TITLE1][TITLE2].